//Doubly linear linked list (Template)
#include<stdio.h>
#include<stdlib.h>

typedef struct Node
{
  int data;
  struct Node *next;
  struct Node *prev;  //#
}NODE, *PNODE,**PNODE;

int main()
{
  PNODE First = NULL;

  return 0;
}